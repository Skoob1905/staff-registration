# Glossary

## Client (also Company/Agency)
An external organisation that staff are assigned to. Each staff member has a single `metadata.assignedToId` + `metadata.assignedToName`.

## Tag
A label (id + value string) that can be attached to staff members, stored as `tags?: string[]` (array of tag IDs) on the `BulkStaff` record.
